<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="32dp"
    android:gravity="center">

    <!-- La caja de texto -->
    <EditText
        android:id="@+id/cajaTexto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Escribe algo aquí..." />

    <!-- El botón original -->
    <Button
        android:id="@+id/botonPasar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Pasar texto"
        android:layout_marginTop="16dp"/>

    <!-- NUEVO: Botón GET -->
    <Button
        android:id="@+id/botonGet"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="GET - Obtener mensajes"
        android:layout_marginTop="16dp"
        android:backgroundTint="#2196F3"/>

    <!-- NUEVO: Botón POST -->
    <Button
        android:id="@+id/botonPost"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="POST - Enviar a API"
        android:layout_marginTop="8dp"
        android:backgroundTint="#4CAF50"/>

    <!-- El label -->
    <TextView
        android:id="@+id/miLabel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="El texto aparecerá aquí"
        android:textSize="18sp"
        android:layout_marginTop="24dp"
        android:gravity="center"/>

</LinearLayout>